% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/yml-dir-set.R
\name{projr_dir_set}
\alias{projr_dir_set}
\alias{projr_dir_set_default}
\title{Set directory configuration}
\usage{
projr_dir_set(
  label,
  path = NULL,
  ignore = NULL,
  ignore_git = NULL,
  ignore_rbuild = NULL,
  git_skip_adjust = NULL,
  simplify_default = TRUE,
  profile = "default"
)

projr_dir_set_default(label, profile = "default")
}
\arguments{
\item{label}{character.
Directory label to configure (e.g., "output", "raw-data", "cache").
Must be a valid directory label.}

\item{path}{character.
Path to the directory.
If \code{NULL}, the setting is not changed.
Default is \code{NULL}.}

\item{ignore}{logical or character.
Whether to ignore the directory in both git and rbuild.
Can be \code{TRUE}, \code{FALSE}, or one of \code{"manual"}, \code{"ignore"}, \code{"no-ignore"}.
If \code{NULL}, the setting is not changed.
Default is \code{NULL}.}

\item{ignore_git}{logical or character.
Whether to ignore the directory in git (.gitignore).
Can be \code{TRUE}, \code{FALSE}, or one of \code{"manual"}, \code{"ignore"}, \code{"no-ignore"}.
If \code{NULL}, the setting is not changed.
Default is \code{NULL}.}

\item{ignore_rbuild}{logical or character.
Whether to ignore the directory in R build (.Rbuildignore).
Can be \code{TRUE}, \code{FALSE}, or one of \code{"manual"}, \code{"ignore"}, \code{"no-ignore"}.
If \code{NULL}, the setting is not changed.
Default is \code{NULL}.}

\item{git_skip_adjust}{logical.
Whether to adjust git skip-worktree flags for files in the directory.
If \code{NULL}, the setting is not changed.
Default is \code{NULL}.}

\item{simplify_default}{logical.
If \code{TRUE}, then if all settings are at their defaults,
the directory configuration is simplified or removed from \verb{_projr.yml}.
Default is \code{TRUE}.}

\item{profile}{character.
Profile to modify.
If \code{"default"} (the default), modifies the default profile (\verb{_projr.yml}).
If \code{NULL}, the active profile is used and written to \verb{_projr.yml}.
If another character vector, modifies \verb{_projr-<profile>.yml}.}
}
\description{
\code{projr_dir_set} sets directory configuration for the project.

The options are:
\itemize{
\item path: the directory path
\item ignore: whether to ignore the directory in both git and rbuild
\item ignore_git: whether to ignore the directory in git (.gitignore)
\item ignore_rbuild: whether to ignore the directory in R build (.Rbuildignore)
\item git_skip_adjust: whether to adjust git skip-worktree flags
}
The default is to leave all the settings unchanged.

If \code{path} is not set and the label is new, an error is thrown.

\code{projr_dir_set_default} removes custom settings for a directory,
reverting to default behavior.
}
\examples{
\dontrun{
# Set path for output directory
projr_dir_set("output", path = "_my_output")

# Set ignore settings for cache directory
projr_dir_set("cache", ignore_git = TRUE, ignore_rbuild = TRUE)

# Create a new directory label
projr_dir_set("results", path = "_results", ignore_git = TRUE)

# Revert to defaults
projr_dir_set_default("cache")
}
}
