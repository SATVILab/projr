# Get the previous version from manifest

Get the previous version from manifest

## Usage

``` r
.build_change_summary_get_previous_version()
```

## Value

Character version string (without 'v' prefix), or NULL if no previous
version.
