# Append message to build log

Append message to build log

## Usage

``` r
.log_build_append(log_file, message, level = "info")
```

## Arguments

- log_file:

  Character. Path to log file.

- message:

  Character. Message to append.

- level:

  Character. Message level (info, debug, success, etc.)
