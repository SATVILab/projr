# Finalize build log with summary

Finalize build log with summary

## Usage

``` r
.log_build_finalize(success = TRUE, start_time = NULL)
```

## Arguments

- success:

  Logical. Whether build succeeded.

- start_time:

  POSIXct. Build start time.
