# Get log directory for build type

Get log directory for build type

## Usage

``` r
.log_dir_get_type(build_type = "output", create = TRUE)
```

## Arguments

- build_type:

  Character. Either "output" or "dev".

- create:

  Logical. Create directory if it doesn't exist.

## Value

Character. Path to the build type log directory.
