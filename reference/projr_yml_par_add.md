# Add the `parameters` key

Add the `parameters` key to the `projr` configuration.

## Usage

``` r
projr_yml_par_add(profile = "default")
```

## Arguments

- profile:

  character. If `NULL`, then the default profile is used. Default is
  `"default"`.
