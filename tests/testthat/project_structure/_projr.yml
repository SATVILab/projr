# default directory settings:
directories:
  data-raw:
    path: _data_raw
  cache: 
    path: _tmp
  output: 
    path: _output
  docs:
    path: docs
build:
  git: true
  github:
    input:
      content: [data-raw]
      description: "Project inputs"
    output:
      content: [output, docs]
      description: "Project outputs"
